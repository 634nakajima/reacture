services:
  - type: web
    name: reacture-server
    runtime: node
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: CLIENT_ORIGIN
        value: https://reacture.vercel.app
      - key: NODE_ENV
        value: production
